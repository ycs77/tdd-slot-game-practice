{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "lint": "eslint . --cache --concurrency auto",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "check": "pnpm lint && pnpm typecheck && pnpm test --run"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@ycs77/eslint-config": "^8.0.0",
    "eslint": "^10.2.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.4"
  }
}
